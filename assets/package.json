{
  "name": "portfolio_theme",
  "version": "1.0.0",
  "description": "Theme Custom Drupal 10",
  "main": "index.js",
  "scripts": {
    "watch": "sass --sourcemap=none --watch sass:css",
    "watch-compressed": "sass --sourcemap=none --watch sass:css --style compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-sass": "^5.1.0"
  }
}
